# Database (for local development)
DATABASE_URL=postgresql://postgres:postgres@localhost:5432/test_writer_db

# MinIO S3 Configuration (for local development)
MINIO_ENDPOINT=http://localhost:9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=test_writer_files

# Docker Environment (override these in docker-compose.yml)
# DATABASE_URL=postgresql://postgres:postgres@postgres:5432/test_writer_db
# MINIO_ENDPOINT=http://minio:9000

# Application
DEBUG=true
MAX_FILE_SIZE=10485760  # 10MB in bytes
ALLOWED_EXTENSIONS=pdf,doc,docx,txt,jpg,jpeg,png
